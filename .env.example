# Copy to .env and fill in. NEVER commit .env (see .gitignore).
#   cp .env.example .env

# --- SEC EDGAR (T1.1) ---
# SEC requires a declared User-Agent identifying you, or they will block you.
# Format: "Name Contact-Email". This is not optional and not a place to be cute.
SEC_USER_AGENT="Ankit Sanjyal asanjyal56@gmail.com"

# --- Model providers ---
# Agent + ablation arms (Sonnet 5) and router (Haiku 4.5)
ANTHROPIC_API_KEY=""
# Judge model — deliberately a different family (DECISIONS.md D-0002 / proposal §8.7)
OPENAI_API_KEY=""

# --- Postgres + pgvector (T1.4) ---
DATABASE_URL="postgresql://filing:filing@localhost:5432/filing_agent"

# --- Langfuse self-hosted tracing (T4.1) ---
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_SECRET_KEY=""
LANGFUSE_HOST="http://localhost:3000"

# --- FastAPI service auth (T2.6) ---
FILING_AGENT_API_KEY=""

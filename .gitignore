# Secrets — never commit (CLAUDE.md §4)
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Cached SEC filings — large, regenerable, and re-downloading them
# would violate our own rate-limit etiquette. Kept local only.
data/raw/
data/cache/
*.sqlite
*.db

# Model weights / embedding caches
models/
.cache/
*.safetensors
*.gguf

# Eval + trace artifacts (results are committed; scratch runs are not)
runs/scratch/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
